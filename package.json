{
  "name": "willo-config",
  "version": "1.0.0",
  "description": "Willo ESLint configurations",
  "main": "index.js",
  "repository": "https://github.com/willo32/willo-eslint",
  "author": "Software devel <software_devel@willo.com>",
  "license": "MIT",
  "private": false,
  "scripts": {
    "lint": "lerna run lint",
    "build": "lerna run build",
    "version": "lerna version --force-publish",
    "publish": "lerna publish -y from-package",
    "bootstrap": "yarn clean && lerna exec yarn && lerna link",
    "clean": "lerna clean -y"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
